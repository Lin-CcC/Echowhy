**【UI/UX 最高指令：实现“认知星轨”沉浸式复盘模式】**

伙伴，我们现有的 UI 骨架和深空星轨主题已完全锁定。现在，我们需要为用户的历史回答记录增加一个极具仪式感、且技术实现极其优雅的**“认知星轨 (Cognitive Star Track)”**微动效。

请严格仔细阅读随附的《最终定稿：认知星轨·沉浸式复盘模式》，并**严格遵守以下 3 条硬核开发约束**，绝对不允许自由发挥：

**一、 严禁使用全局遮罩与复杂 Z-index（实现局部聚光）**

- **绝对禁止**在全屏覆盖暗色 `div`。
- **指定实现方式**：当点击“✨ 星轨复盘”时，通过状态管理（如 React state），动态为当前问题框**之上和之下**的兄弟文本元素添加 `opacity-20 transition-opacity duration-200` 类名。
- 必须确保左侧侧边栏、右侧 Source 面板的 DOM 结构和样式完全不参与变暗，保持绝对的原样与稳定。

**二、 严守单色冷光源体系（禁止交通灯配色）**

- 历史尝试的节点绝不允许出现红、黄、绿等杂色。
- **强制色阶**：
  1. 首次/错误尝试：`text-cyan-900`（极暗星）。
  2. 修正尝试：`text-cyan-600`（半亮星）。
  3. 最终正确：`text-cyan-400` 配合 `[text-shadow:0_0_10px_#22d3ee]`（耀眼核心星与光晕）。
- 连线必须是纯折线/直线（`border-cyan-800` 到 `border-cyan-400` 的递进）。

**三、 400ms-600ms 电涌级联动画（禁止冗长拖沓）**

- 动画必须迅捷、凌厉。请使用 CSS Animation 或 Framer Motion 的 `staggerChildren` 级联延迟特性。
- 第一颗星点亮，折线迅速延展，紧接着第二颗星点亮。总时间严格控制在 600ms 以内，拒绝任何拖泥带水的缓慢渐显。

**请务必在不破坏现有三栏布局的前提下，通过局部透明度和极速动效，实现这种“拔剑出鞘”般的星空点亮仪式！期待你的代码实现！**

# 最终定稿：认知星轨・沉浸式复盘模式（无隐患・高级版）

## 核心原则

坚守深空冷色光学基调 + 高效学习无效率惩罚 + 技术实现优雅无层级冲突 + 折线星轨不改动 + 右侧代码区职责单一不杂糅

## 一、触发交互（极简无侵入）

折叠问题标题旁，保留**星空风微图标：✨ 星轨复盘**

点击即触发复盘模式，无多余操作

## 二、视觉聚光方案（解决全局变暗隐患・局部褪色聚光灯）

1. 不使用全屏遮罩 / 复杂 z-index，仅对**当前问题上下的正文内容**做「褪色处理」（opacity-20）
2. 左侧星座链、右侧源码工作台**完全保持原样**，布局稳定、点击 / 滚动无任何异常
3. 视觉焦点被强行聚焦在当前问题的「认知星轨」上，聚焦感拉满且技术实现极简

## 三、星轨色彩体系（解决交通灯崩塌・纯冷青单色阶）

全程只用**青蓝 (Cyan) 单色系**，通过**亮度、光晕、星轨粗细**区分认知阶段，无任何红黄杂色，坚守深空清冷高级感：

1. **Weak（首次尝试・混沌）**：极微弱星点 `cyan-900`，几乎融入背景，无明显光晕
2. **Partial（修正尝试・清晰）**：半亮星点 `cyan-600`，带细微折线连线
3. **Strong（最终掌握・顿悟）**：耀眼核心亮星 `cyan-400`，带白色柔和光晕 + 最粗实线星轨

## 四、动画效果（解决效率惩罚・电涌式级联动画）

总时长控制在**400ms-600ms**，干脆利落，兼顾仪式感与学习效率：

1. 200ms：当前问题上下正文快速褪色

2. 级联点亮：星轨折线如电流般极速划过，依次点亮每颗尝试星点

3. 收尾爆发：最终「Strong 亮星」闪现核心光晕，星轨完全成型

   

   全程无拖沓，第一次惊艳、反复使用也无等待折磨

## 五、星轨形态与内容

1. 星轨全程为**直线 / 折线**，严格遵循星空主题，无任何曲线
2. 星点下方悬浮极淡微光文字，展示每次尝试的答案 + AI 提示，不遮挡星轨
3. 内容与思考轨迹完全绑定，清晰呈现从混沌到顿悟的全过程

## 六、退出方式

点击空白处 / 再次点击「✨ 星轨复盘」→ 200ms 快速恢复正文透明度，星轨柔和隐去，界面回归极简深空状态

------

## 最终效果总结

这个方案彻底规避了所有隐患：

✅ 无色彩体系崩塌，全程冷色星空质感，高级不俗气

✅ 无效率惩罚，动画迅捷不拖沓，适配高频复习场景

✅ 无前端技术坑，局部褪色实现聚光，布局交互零异常

✅ 坚守所有设计红线，右侧不杂糅、星轨为折线、阅读流干净

✅ 打造出 Echowhy 独有的「顿悟时刻」，用户能清晰看到自己的思维碎片被星轨串联，获得极致成长满足感

这个定稿完全符合我对「沉浸式星空复盘」的所有期待，可以直接转化为代码指令！